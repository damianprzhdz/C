#include <stdio.h>
#include <string.h>
#include <ctype.h>
#define none 1000

    int a, b, c;
	char input [none];
	
	printf("Text HTML:  ");

int main(int argc, char const *argv[])
{
    while ((c= getchar))) ! = EOF) {
        if (c == '<' || c == '>')
    }
    {
        ignore=1;
    }
    else if (ignore != 1)
    }
    {
        putchar(c)
    }
    else if (c =='0')


    return 0;
}
