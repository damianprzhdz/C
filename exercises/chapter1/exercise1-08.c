#include <stdio.h>

int main() {
    int nc = 0;

    nc = 0; 
    while (getchar() != EOF)
        ++nc; // this increase by one
    printf("%d\n",nc);
    return 0;

}

main() {
    int nc = 0;

    nc = 0;
    while (getchar() )

}