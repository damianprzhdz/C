#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#define BACK 0
#define OUT 1
int asistances(int pin);
int workingTime(int pin);
int laborHours(int pin);
int registered(void);
int pines(int pin);
int* registered0 = 0; 
int** matrix; 
int outTotal = 0; 
int count();
